import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};
const defaultProps = {
};

const InspectDistributor = ({ }) => (
  <div>
    InspectDistributor
  </div>
);

InspectDistributor.propTypes = propTypes;
InspectDistributor.defaultProps = defaultProps;
export default InspectDistributor;
