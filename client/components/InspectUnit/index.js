import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};
const defaultProps = {
};

const InspectUnit = ({ }) => (
  <div>
    InspectUnit
  </div>
);

InspectUnit.propTypes = propTypes;
InspectUnit.defaultProps = defaultProps;
export default InspectUnit;
