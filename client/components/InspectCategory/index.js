import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {
};
const defaultProps = {
};

const InspectCategory = ({ }) => (
  <div>
    Category
  </div>
);

InspectCategory.propTypes = propTypes;
InspectCategory.defaultProps = defaultProps;
export default InspectCategory;
