import React from 'react';
import PropTypes from 'prop-types';
const propTypes = {
};
const defaultProps = {
};
const InspectCompany = ({ }) => (
  <div>
    Company
  </div>
);
InspectCompany.propTypes = propTypes;
InspectCompany.defaultProps = defaultProps;
export default InspectCompany;
